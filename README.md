# Web Engineering Lab

**Student:** Muhammad Tayyab  
**CMS ID:** 023-24-0118  
**Semester:** 5th Semester  

## Overview
This repository contains my lab work and submissions for Web Engineering:

- **Week 01:** How the Web Works, URL anatomy, HTTP status codes, DevTools, and first HTML page (`hello-web/index.html`).
- **Week 02:** HTML5 semantic elements, `profile.html`, skills table, contact form with validation, and homework rewrite.
- **Week 03:** CSS fundamentals, external stylesheets, Box Model, Selector scavenger hunt, and Specificity duel.
- **Week 04:** CSS Layout: Flexbox vs Grid (1D vs 2D), wrapping flex gallery, space distribution with `justify-content`, responsive CSS Grid (`auto-fit` + `minmax`), mobile-first breakpoints, and CSS positioning (`static`, `relative`, `absolute`, `fixed`, `sticky`). Homework Tasks: Grey-box layout clone (`greybox-clone.html`), mobile-first responsive profile (`responsive-profile.html`), and layout debugging break & fix (`layout-debug.html`).
- **Week 05:** JavaScript Fundamentals: Pure Node.js scripts without browser DOM, `const` / `let` declaration scoping without `var`, loose (`==`) vs strict (`===`) equality and type coercion, string utilities (`getInitials`, `isPalindrome`, `countWords`), conditional grading and nullish coalescing (`??`), non-mutating array pipelines (`map`, `filter`, `reduce`, `some`, `every`), object destructuring, spread operator, and `groupBy` cataloging. Robust restaurant tip calculator (`calculateBill`) with defensive validation and `try/catch` error containment. Homework tasks: Gradebook analysis (`gradebook.js`), JavaScript execution predictions note (`predict.js` / `predict.txt`), freeCodeCamp Basic JavaScript mastery (`freecodecamp.md`), and tip calculator edge cases with multi-line receipt formatting (`calculator.js`).
